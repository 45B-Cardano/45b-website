# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Common Examples
**/keys**

# JS / TS Examples
**/node_modules

# vscode
.vscode

# markdownlint
.markdownlint.json

# intellij
.idea

# (only check in yarn.lock when deliberately establishing a software baseline)
yarn.lock

# Temporary files that sometimes get checked in by accident
.yarn/**

# Lucas's inbox for content destined for the site — not part of the site
# itself, just a handoff staging area (see notes/45b-org.md in the Web3 repo)
/INBOX

# Temporary snapshots pulled from the live host for comparison — not repo content
/.live-web3-snapshot

# Copy of the last build actually deployed, kept so the next deploy can diff
# against it and upload only changed files (see notes/45b-org.md)
/.last-deploy
